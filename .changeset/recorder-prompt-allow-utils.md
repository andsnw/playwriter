---
'playwriter': patch
---

Allow recorded skills to ship an importable `utils.js` again. The recorder prompt still asks for markdown playwriter examples in `SKILL.md`, and also a utils script so replay costs fewer tokens.
