---
'playwriter': patch
---

Point the recorder skill prompt at the Agent Skills default locations instead of OpenCode-only paths.

Personal skills go in `~/.agents/skills/<name>/`. Project skills go in `.agents/skills/<name>/` in the current repo.
